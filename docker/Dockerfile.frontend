FROM node:20-alpine

# Set working directory
WORKDIR /app

# Expose port
EXPOSE 3000

# Start development server with inline command
CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0 --port 3000"]